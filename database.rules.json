{
  "rules": {
    ".read": "auth != null",   // Only authenticated users can read data
    ".write": "auth != null",  // Only authenticated users can write data
    "files": {
      "$fileId": {
        ".validate": "newData.hasChildren(['fileName', 'fileUrl', 'uploadedAt'])"
      }
    }
  }
}
